web:
   image: php:7.2-apache
   environment:
     - ALLOW_OVERRIDE=true
   ports:
     - "80:80"
   links:
     - db
   volumes:
     - ./app:/var/www/html/
     - ./app/php:/var/php/

db:
  image: mariadb
  restart: always
  volumes:
    - ./mysql:/var/lib/mysql
  environment:
    MYSQL_ROOT_PASSWORD: toor
    MYSQL_USER: admin
    MYSQL_PASSWORD: test
    MYSQL_DATABASE: blog
  ports:
    - "8889:3306"
